#include <stdio.h>
#include <locale.h>

int main() {
    int n;
    setlocale(LC_ALL, "Russian");

    printf("Введите число: ");
    scanf("%d", &n);

    if (n % 2 == 0) {
        printf("Чётное\n");
    } else {
        printf("Нечётное\n");
    }

    return 0;
}
